name: Test Action

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-linux:
    steps:
      - uses: actions/checkout@v3

      - name: Run Action
        uses: ./